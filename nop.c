#include "monty.h"

/**
 * nop - opcode nop does nothing
 * @stack: the stack
 * @line_number: line number
 * Return: void
 */

void nop(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}
